<p><%= t('global.greeting').join("#{ @resource.email}")%>!</p>

<p> <%= t('forms.mailer.reset_password.message') %></p>

<p><%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token) %></p>

<p> <%= t('forms.mailer.password_change.info') %></p>
