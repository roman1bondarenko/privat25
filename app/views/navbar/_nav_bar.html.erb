<nav class="navbar navbar-light bg-light">
  <a class="navbar-brand" href="/">
    <img src="<%= asset_path "logo.png" %>" alt="Logo" style="width:25%;">
  </a>
  <form class="form-inline">
    <div class="nav-section" style="margin-right: 20px">
      <%= link_to_unless_current "English", locale: "en" %> |
      <%= link_to_unless_current "Русский", locale: "ru" %>
    </div>
    <br>
    <% if user_signed_in? %>
      <a class="nav-item" style="margin-right: 20px"> <%= t 'nav-bar.login-as' %>  <%= get_current_user_name %></a>
      <a class="btn btn-outline-primary" <%= link_to(t('nav-bar.logout'), destroy_user_session_path, method: :delete) %>
    <% else %>
      <%= link_to(t('nav-bar.sign-in'), new_user_session_path, class: "btn btn-outline-primary", role: "button") %>
      <div style="padding-left: 10px;">
        <%= link_to(t('nav-bar.sign-up'), new_user_registration_path, class: "btn btn-outline-success", role: "button") %>
      </div>
    <% end %>
  </form>
</nav>